export const LOGIN = 'LOGIN'

export const login = () => dispatch =>{
    return dispatch({
        type: LOGIN
    })
}

export const checkLogin = () => dispatch =>{
    return dispatch({
        type: LOGIN
    })
}